- var title = is_home()?`${config.title} - ${config.subtitle}`:`${page.title} - ${config.title}`
html
    head
        title= title
        meta(charset='UTF-8')
        meta(name="generator" content="为什么hexo要在我的head里插这个我不喜欢诶")
        meta(name="viewport" content="width=device-width, initial-scale=1")
        link(rel='stylesheet', href=url_for('styles/main.css'))
        link(rel="shortcut icon" type="image/ico" href="/assets/images/favicon.ico")
    body
        .container
            include header.pug
            block content
            include footer.pug
        script(async='' src='https://www.googletagmanager.com/gtag/js?id=UA-101983219-2')
        script.
            window.dataLayer = window.dataLayer || [];
            function gtag(){dataLayer.push(arguments);}
            gtag('js', new Date());
            gtag('config', 'UA-101983219-2');